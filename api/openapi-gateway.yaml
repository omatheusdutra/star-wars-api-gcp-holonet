swagger: "2.0"
info:
  title: Holonet Galactic Console API
  version: "1.0.0"
  description: API Gateway spec for Holonet Galactic Console (Cloud Run backend + API Gateway).
schemes:
  - https
produces:
  - application/json
paths:
  /:
    get:
      operationId: root
      security: []
      x-google-backend:
        address: ${backend_url}
        jwt_audience: ${backend_url}
        path_translation: APPEND_PATH_TO_ADDRESS
        deadline: 60.0
      responses:
        "200":
          description: OK
  /docs:
    get:
      operationId: docs
      security: []
      x-google-backend:
        address: ${backend_url}
        jwt_audience: ${backend_url}
        path_translation: APPEND_PATH_TO_ADDRESS
        deadline: 60.0
      responses:
        "200":
          description: OK
  /docs/oauth2-redirect:
    get:
      operationId: docsOAuth2Redirect
      security: []
      x-google-backend:
        address: ${backend_url}
        jwt_audience: ${backend_url}
        path_translation: APPEND_PATH_TO_ADDRESS
        deadline: 60.0
      responses:
        "200":
          description: OK
  /openapi.json:
    get:
      operationId: openapiJson
      security: []
      x-google-backend:
        address: ${backend_url}
        jwt_audience: ${backend_url}
        path_translation: APPEND_PATH_TO_ADDRESS
        deadline: 60.0
      responses:
        "200":
          description: OK
  /redoc:
    get:
      operationId: redoc
      security: []
      x-google-backend:
        address: ${backend_url}
        jwt_audience: ${backend_url}
        path_translation: APPEND_PATH_TO_ADDRESS
        deadline: 60.0
      responses:
        "200":
          description: OK
  /films:
    get:
      operationId: publicFilms
      security: []
      x-google-backend:
        address: ${backend_url}
        jwt_audience: ${backend_url}
        path_translation: APPEND_PATH_TO_ADDRESS
        deadline: 60.0
      parameters:
        - name: q
          in: query
          type: string
        - name: search
          in: query
          type: string
        - name: page
          in: query
          type: integer
        - name: page_size
          in: query
          type: integer
        - name: all
          in: query
          type: boolean
        - name: sort
          in: query
          type: string
        - name: order
          in: query
          type: string
        - name: order_by
          in: query
          type: string
        - name: reverse
          in: query
          type: boolean
        - name: fields
          in: query
          type: string
      responses:
        "200":
          description: OK
  /characters:
    get:
      operationId: publicCharacters
      security: []
      x-google-backend:
        address: ${backend_url}
        jwt_audience: ${backend_url}
        path_translation: APPEND_PATH_TO_ADDRESS
        deadline: 60.0
      parameters:
        - name: q
          in: query
          type: string
        - name: search
          in: query
          type: string
        - name: page
          in: query
          type: integer
        - name: page_size
          in: query
          type: integer
        - name: all
          in: query
          type: boolean
        - name: sort
          in: query
          type: string
        - name: order
          in: query
          type: string
        - name: order_by
          in: query
          type: string
        - name: reverse
          in: query
          type: boolean
        - name: fields
          in: query
          type: string
      responses:
        "200":
          description: OK
  /planets:
    get:
      operationId: publicPlanets
      security: []
      x-google-backend:
        address: ${backend_url}
        jwt_audience: ${backend_url}
        path_translation: APPEND_PATH_TO_ADDRESS
        deadline: 60.0
      parameters:
        - name: q
          in: query
          type: string
        - name: search
          in: query
          type: string
        - name: page
          in: query
          type: integer
        - name: page_size
          in: query
          type: integer
        - name: all
          in: query
          type: boolean
        - name: sort
          in: query
          type: string
        - name: order
          in: query
          type: string
        - name: order_by
          in: query
          type: string
        - name: reverse
          in: query
          type: boolean
        - name: fields
          in: query
          type: string
      responses:
        "200":
          description: OK
  /starships:
    get:
      operationId: publicStarships
      security: []
      x-google-backend:
        address: ${backend_url}
        jwt_audience: ${backend_url}
        path_translation: APPEND_PATH_TO_ADDRESS
        deadline: 60.0
      parameters:
        - name: q
          in: query
          type: string
        - name: search
          in: query
          type: string
        - name: page
          in: query
          type: integer
        - name: page_size
          in: query
          type: integer
        - name: all
          in: query
          type: boolean
        - name: sort
          in: query
          type: string
        - name: order
          in: query
          type: string
        - name: order_by
          in: query
          type: string
        - name: reverse
          in: query
          type: boolean
        - name: fields
          in: query
          type: string
      responses:
        "200":
          description: OK
  /vehicles:
    get:
      operationId: publicVehicles
      security: []
      x-google-backend:
        address: ${backend_url}
        jwt_audience: ${backend_url}
        path_translation: APPEND_PATH_TO_ADDRESS
        deadline: 60.0
      parameters:
        - name: q
          in: query
          type: string
        - name: search
          in: query
          type: string
        - name: page
          in: query
          type: integer
        - name: page_size
          in: query
          type: integer
        - name: all
          in: query
          type: boolean
        - name: sort
          in: query
          type: string
        - name: order
          in: query
          type: string
        - name: order_by
          in: query
          type: string
        - name: reverse
          in: query
          type: boolean
        - name: fields
          in: query
          type: string
      responses:
        "200":
          description: OK
  /species:
    get:
      operationId: publicSpecies
      security: []
      x-google-backend:
        address: ${backend_url}
        jwt_audience: ${backend_url}
        path_translation: APPEND_PATH_TO_ADDRESS
        deadline: 60.0
      parameters:
        - name: q
          in: query
          type: string
        - name: search
          in: query
          type: string
        - name: page
          in: query
          type: integer
        - name: page_size
          in: query
          type: integer
        - name: all
          in: query
          type: boolean
        - name: sort
          in: query
          type: string
        - name: order
          in: query
          type: string
        - name: order_by
          in: query
          type: string
        - name: reverse
          in: query
          type: boolean
        - name: fields
          in: query
          type: string
      responses:
        "200":
          description: OK
  /health:
    get:
      operationId: health
      x-google-backend:
        address: ${backend_url}
        jwt_audience: ${backend_url}
        path_translation: APPEND_PATH_TO_ADDRESS
        deadline: 60.0
      responses:
        "200":
          description: OK
  /v1/health:
    get:
      operationId: healthV1
      x-google-backend:
        address: ${backend_url}
        jwt_audience: ${backend_url}
        path_translation: APPEND_PATH_TO_ADDRESS
        deadline: 60.0
      responses:
        "200":
          description: OK
  /v1/meta:
    get:
      operationId: meta
      x-google-backend:
        address: ${backend_url}
        jwt_audience: ${backend_url}
        path_translation: APPEND_PATH_TO_ADDRESS
        deadline: 60.0
      responses:
        "200":
          description: OK
  /v1/search:
    get:
      operationId: search
      x-google-backend:
        address: ${backend_url}
        jwt_audience: ${backend_url}
        path_translation: APPEND_PATH_TO_ADDRESS
        deadline: 60.0
      parameters:
        - name: resource
          in: query
          required: true
          type: string
        - name: q
          in: query
          type: string
        - name: search
          in: query
          type: string
        - name: page
          in: query
          type: integer
        - name: page_size
          in: query
          type: integer
        - name: sort
          in: query
          type: string
        - name: order
          in: query
          type: string
        - name: order_by
          in: query
          type: string
        - name: reverse
          in: query
          type: boolean
        - name: fields
          in: query
          type: string
      responses:
        "200":
          description: OK
  /v1/films/{id}:
    get:
      operationId: getFilm
      x-google-backend:
        address: ${backend_url}
        jwt_audience: ${backend_url}
        path_translation: APPEND_PATH_TO_ADDRESS
        deadline: 60.0
      parameters:
        - name: id
          in: path
          required: true
          type: integer
      responses:
        "200":
          description: OK
  /v1/people/{id}:
    get:
      operationId: getPerson
      x-google-backend:
        address: ${backend_url}
        jwt_audience: ${backend_url}
        path_translation: APPEND_PATH_TO_ADDRESS
        deadline: 60.0
      parameters:
        - name: id
          in: path
          required: true
          type: integer
      responses:
        "200":
          description: OK
  /v1/planets/{id}:
    get:
      operationId: getPlanet
      x-google-backend:
        address: ${backend_url}
        jwt_audience: ${backend_url}
        path_translation: APPEND_PATH_TO_ADDRESS
        deadline: 60.0
      parameters:
        - name: id
          in: path
          required: true
          type: integer
      responses:
        "200":
          description: OK
  /v1/starships/{id}:
    get:
      operationId: getStarship
      x-google-backend:
        address: ${backend_url}
        jwt_audience: ${backend_url}
        path_translation: APPEND_PATH_TO_ADDRESS
        deadline: 60.0
      parameters:
        - name: id
          in: path
          required: true
          type: integer
      responses:
        "200":
          description: OK
  /v1/films/{id}/characters:
    get:
      operationId: getFilmCharacters
      x-google-backend:
        address: ${backend_url}
        jwt_audience: ${backend_url}
        path_translation: APPEND_PATH_TO_ADDRESS
        deadline: 60.0
      parameters:
        - name: id
          in: path
          required: true
          type: integer
      responses:
        "200":
          description: OK
  /v1/people/{id}/films:
    get:
      operationId: getPersonFilms
      x-google-backend:
        address: ${backend_url}
        jwt_audience: ${backend_url}
        path_translation: APPEND_PATH_TO_ADDRESS
        deadline: 60.0
      parameters:
        - name: id
          in: path
          required: true
          type: integer
      responses:
        "200":
          description: OK
  /v1/graph:
    get:
      operationId: getGraph
      x-google-backend:
        address: ${backend_url}
        jwt_audience: ${backend_url}
        path_translation: APPEND_PATH_TO_ADDRESS
        deadline: 60.0
      responses:
        "200":
          description: OK
  /v1/planets/map:
    get:
      operationId: getPlanetsMap
      x-google-backend:
        address: ${backend_url}
        jwt_audience: ${backend_url}
        path_translation: APPEND_PATH_TO_ADDRESS
        deadline: 60.0
      responses:
        "200":
          description: OK
securityDefinitions:
  apiKeyAuth:
    type: apiKey
    in: header
    name: x-api-key
security:
  - apiKeyAuth: []





